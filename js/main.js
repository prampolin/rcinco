


//let isOdd = (n) => n % 2 == 0 ? false : true; 

let totalArr = (arr) => arr.length;

//let amountAllowed = (n/*, max, obj*/) => n.length /*== max ? $(obj).attr('disabled', false) : $(obj).attr('disabled', true)*/; 

let amountSelect = (w, t) => ($(w).length == t) ? true : false;

let offCheckbox = (w, v) => (v == true) ? $(w).attr('disabled', true) : $(w).attr('disabled', false);

